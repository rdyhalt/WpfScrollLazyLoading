<Window x:Class="WpfScrollLazyLoading.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:WpfScrollLazyLoading.ViewModel"
        xmlns:wpfScrollLazyLoading="clr-namespace:WpfScrollLazyLoading"

        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModel:DesignMainViewModel, IsDesignTimeCreatable=True}"
       
        Title="MainWindow" Height="350" Width="525"
        >
    <DockPanel>

        <TextBlock DockPanel.Dock="Top" Margin="5" >Total count: <Run Text="{Binding Path=TotalCount, Mode=OneWay}" /> - Current count: <Run Text="{Binding Path=Items.Count, Mode=OneWay}"/></TextBlock>

        <!--<ScrollViewer wpfScrollLazyLoading:ScrollHelper.ScrollToBottom="{Binding Path=ChunkLoaderCommand}">-->
            <ListBox DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding Path=Items}"
                     
                     wpfScrollLazyLoading:ScrollHelper.ScrollToBottom="{Binding Path=ChunkLoaderCommand}">



            </ListBox>
        <!--</ScrollViewer>-->

    </DockPanel>
</Window>



